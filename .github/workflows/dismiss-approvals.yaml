# This file has been autogenerated via Opentofu or, if it's in the
# workflows directory has been copied.
# Run `cp .github/generated/dismiss-approvals.yaml .github/workflows/dismiss-approvals.yaml`
# and raise a PR to ensure stale Graphite reviews are correctly dismissed.
name: dismiss_stale_pr_reviews
on:
  pull_request:
    types:
    - opened
    - synchronize
    - reopened
permissions:
  actions: read
  contents: read
  pull-requests: write
jobs:
  dismiss_stale_approvals:
    runs-on: ubuntu-latest
    steps:
    - name: Dismiss stale pull request approvals
      uses: withgraphite/dismiss-stale-approvals@main
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
